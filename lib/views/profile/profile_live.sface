<main
  data-id="profile"
  data-user-id={e(@user, :id, "")}
  class={"max-w-[680px] w-full mx-auto": @is_guest?}
>
  <StatelessComponent
    module={Config.get([:ui, :profile, :sections, tab(@selected_tab)], Bonfire.UI.Social.ProfileTimelineLive)}
    {...assigns}
  >
    <:header>
      {#if @character_type == :user and @is_guest?}
        <Bonfire.UI.Me.GuestProfileLive
          boundary_preset={@boundary_preset}
          user={@user}
          id="guest-profile"
        />

        <div class="font-medium mt-10 text-lg mb-4 capitalize">{@selected_tab}</div>
      {#else}
        <StatefulComponent
          module={Config.get(
            [:ui, :profile, :hero_component],
            Bonfire.UI.Me.ProfileHeroFullLive
          )}
          id={Text.random_string()}
          object_id={id(@user)}
          object={@user}
          boundary_preset={@boundary_preset}
          user={@user}
          path={@path}
          block_status={e(assigns, :block_status, nil)}
          selected_tab={@selected_tab}
          follows_me={e(@follows_me, false)}
          character_type={@character_type}
        />
      {/if}
    </:header>
  </StatelessComponent>
</main>
